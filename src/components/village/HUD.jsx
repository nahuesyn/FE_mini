// HUD (Heads-Up Display) — 마을 전역 UI 컴포넌트
// - 현재 위치 표시 (breadcrumb)
// - 낮/밤 토글 버튼
// - 방문한 구역 뱃지
// [SkyviewButton]은 layout.js 에서 전역으로 관리 (fixed bottom-6 right-6)

export default function HUD() {
  return <div>HUD</div>;
}
